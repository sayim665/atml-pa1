method: sam
seed: 6304
rho: 0.05
lr: 0.0001
weight_decay: 0.0001
max_epochs: 30
early_stop_patience: 5
batch_size_per_source_domain: 8
optimizer_pattern: "standard 2-step SAM (Foret et al. 2021), AdamW base optimizer"
attribution: "SAM optimizer wrapper follows the reference pattern used in github.com/davda54/sam"
note: "Sketch is never loaded by this method at any stage."
